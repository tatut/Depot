"
This package contains the field definitions to map fields in
entity classes to XTDB attributes.

Depot enables more detailed type and constraints than simply the 
raw XTDB client mapping.
"
Class {
	#name : #ManifestDepotEntity,
	#superclass : #PackageManifest,
	#category : #'Depot-Entity-Manifest'
}
